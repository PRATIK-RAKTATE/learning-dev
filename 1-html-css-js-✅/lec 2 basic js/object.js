function greetUser(user) {
    console.log("hi", user.name, "your age is", user.age);

}

const user = {
    name : "pratik",
    age : 19
}

greetUser(user);

// we can create array of different types of values
let arr = ["pratik", 13, true]